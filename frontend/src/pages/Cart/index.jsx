import classNames from "classnames/bind";
import styles from './Cart.module.scss'

const cx = classNames.bind(styles)

function CartPage() {
  return <div classNames={cx('container')}>
    <div classNames={cx('wrapper')}>
  
    </div>
  </div>;
}

export default CartPage;
